@model IEnumerable<MacLibraryProject.Models.Test>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Test_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Test_Dept)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Test_Review)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Test_Pic)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Test_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Test_Dept)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Test_Review)
        </td>
        <td>
            @*@Html.DisplayFor(modelItem => item.Test_Pic)*@
            <img src="@Url.Content(item.Test_Pic)" alt="Image Description" width="50px" >

        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Test_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Test_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Test_id })
        </td>
    </tr>
}

</table>
